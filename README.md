# 📅 Assignment Manager

یک اپلیکیشن دسکتاپ سبک و قدرتمند برای مدیریت تکالیف و پروژه‌های دانشگاهی با پشتیبانی کامل از **تاریخ شمسی** و اولویت‌بندی تسک‌ها.

![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)
![PySide6](https://img.shields.io/badge/UI-PySide6-green.svg)
![SQLite](https://img.shields.io/badge/Database-SQLite-lightgrey.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

## ✨ قابلیت‌ها
- **تقویم جلالی:** ثبت و نمایش ددلاین‌ها به صورت شمسی.
- **سیستم هشدار رنگی:** - 🔴 ددلاین‌های کمتر از ۳ روز (بحرانی)
  - 🟡 ددلاین‌های کمتر از ۷ روز (نزدیک)
- **مدیریت دیتابیس:** استفاده از SQLite برای ذخیره‌سازی آفلاین و سریع.
- **رابط کاربری مدرن:** طراحی Dark Mode با انیمیشن‌های تعاملی (Refresh Animation).
- **اعتبارسنجی ورودی:** جلوگیری از ورود داده‌های اشتباه با استفاده از Masking و Regex.

## 📸 پیش‌نمایش
![App Screenshot](screenshots/img.png)
## 🛠 نصب و اجرا

۱. ابتدا مخزن را کلون کنید:
```bash
    git clone ...
    cd AssignmentManager
```
۲. یک محیط مجازی (Virtual Env) ساخته و فعال کنید:

```bash
    python -m venv .venv
    source .venv/bin/activate  # در ویندوز: .venv\Scripts\activate
```

۳. نیازمندی‌ها را نصب کنید:

```bash
    pip install -r requirements.txt
```

۴. برنامه را اجرا کنید:

```Bash
    python UI.py
```
📦 خروجی گرفتن (Executable)

برای ساخت فایل اجرایی مستقل (بدون نیاز به نصب پایتون)، از Nuitka استفاده شده است:

```bash
    python -m nuitka --standalone --onefile --plugin-enable=pyside6 --disable-console --include-data-dir=icons=icons UI.py
```
📂 ساختار پروژه

    UI.py: مدیریت رابط کاربری و تعاملات PySide6.

    core.py: منطق کار با دیتابیس و تبدیل تاریخ‌های شمسی/میلادی.

    icons/: فایل‌های گرافیکی برنامه.

    assignments.db: دیتابیس محلی (پس از اولین اجرا ساخته می‌شود).

🤝 مشارکت

اگر پیشنهادی برای بهبود برنامه دارید یا باگی پیدا کردید، خوشحال می‌شوم یک Issue باز کنید یا یک Pull Request بفرستید.
