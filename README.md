# 📅 Assignment Manager

یک اپلیکیشن دسکتاپ سبک و ساده برای مدیریت تکالیف و پروژه‌های دانشگاهی با پشتیبانی کامل از **تاریخ شمسی** و اولویت‌بندی تسک‌ها.

![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)
![PySide6](https://img.shields.io/badge/UI-PySide6-green.svg)
![SQLite](https://img.shields.io/badge/Database-SQLite-lightgrey.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

## ✨ قابلیت‌ها
- **تقویم جلالی:** ثبت و نمایش ددلاین‌ها به صورت شمسی.
- **سیستم هشدار رنگی:** - 🔴 کمتر از ۳ روز (بحرانی)
  - 🟡 کمتر از ۷ روز (نزدیک)
- **پایداری داده‌ها:** ذخیره‌سازی دیتابیس در مسیر استاندارد سیستم (`~/.local/share`) برای جلوگیری از حذف داده‌ها با جابه‌جایی فایل اجرایی.

## 📸 پیش‌نمایش
![App Screenshot](screenshots/img.png)

## 🛠 نصب و اجرا (برای توسعه‌دهندگان)

۱. ابتدا مخزن را کلون کنید:
```bash
  git clone [https://github.com/Navid535/Assignment-Manager.git](https://github.com/Navid535/Assignment-Manager.git)
  cd Assignment-Manager
```
۲. محیط مجازی را ساخته و فعال کنید:
```Bash
  python -m venv .venv
  source .venv/bin/activate  # در ویندوز: .venv\Scripts\activate
```
۳. نصب نیازمندی‌ها و اجرا:
```Bash
  pip install -r requirements.txt
  python UI.py
```
📦 استفاده از نسخه اجرایی (Standalone)

اگر نمی‌خواهید پایتون نصب کنید، می‌توانید فایل اجرایی را از بخش Releases دانلود کنید.
اجرا در لینوکس:

۱. به فایل مجوز اجرا بدهید:
```Bash
  chmod +x AssignmentManager.bin
```
۲. برای راحتی در اجرا و داشتن آیکون در منوی برنامه‌ها، می‌توانید اسکریپت install.sh موجود در مخزن را اجرا کنید:

```Bash
  bash install.sh
```
🏗 خروجی گرفتن مجدد

برای کامپایل پروژه با استفاده از Nuitka:
```Bash
  python -m nuitka --standalone --onefile --plugin-enable=pyside6 --disable-console --include-data-dir=icons=icons --output-filename=AssignmentManager.bin UI.py
```
📂 ساختار پروژه

    UI.py: مدیریت رابط کاربری و تعاملات PySide6.

    core.py: منطق دیتابیس و مدیریت تاریخ‌های جلالی.

    icons/: آیکون‌های برنامه (SVG/PNG).

    install.sh: اسکریپت نصب خودکار برای لینوکس.

    screenshots/: تصاویر محیط برنامه.

🤝 مشارکت

اگر پیشنهادی برای بهبود برنامه دارید، خوشحال می‌شوم یک Issue باز کنید یا یک Pull Request بفرستید.
